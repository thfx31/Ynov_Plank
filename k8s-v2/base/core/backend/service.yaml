apiVersion: v1
kind: Service
metadata:
  name: algohive-server
  namespace: algohive
  labels:
    app.kubernetes.io/name: algohive-server
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: algohive
spec:
  selector:
    app: algohive-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
