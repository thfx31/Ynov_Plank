apiVersion: apps/v1
kind: Deployment
metadata:
  name: beeapi-server-montpellier
  namespace: algohive
  labels:
    app.kubernetes.io/instance: montpellier
spec:
  selector:
    matchLabels:
      app: beeapi-server-montpellier
  template:
    metadata:
      labels:
        app: beeapi-server-montpellier
        app.kubernetes.io/instance: montpellier
    spec:
      containers:
        - name: beeapi
          image: ghcr.io/algohive-coding-puzzles/beeapi-go:latest
          ports:
            - containerPort: 5000
          env:
            - name: SERVER_NAME
              value: "Ynov-Montpellier"
          volumeMounts:
            - name: puzzles-storage
              mountPath: /app/puzzles
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: puzzles-storage
          persistentVolumeClaim:
            claimName: puzzles-montpellier-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: beeapi-server-montpellier
  namespace: algohive
  labels:
    app.kubernetes.io/instance: montpellier
spec:
  selector:
    app: beeapi-server-montpellier
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: puzzles-montpellier-pvc
  namespace: algohive
  labels:
    app.kubernetes.io/instance: montpellier
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  # storageClassName: gp3
